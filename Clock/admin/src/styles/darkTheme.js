import { createTheme } from '@mui/material/styles';

export const darkTheme = createTheme({
  palette: {
    primary: {
      main: '#4F5686',
    },
    secondary: {
      main: '#F0A8B9',
    },
  },
});
